{
  "name": "crypto-trading-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd bot && npm install && cd ../dashboard && npm install",
    "build:bot": "cd bot && npm run build",
    "build:dashboard": "cd dashboard && npm run build",
    "build": "npm run build:bot && npm run build:dashboard",
    "start:bot": "cd bot && npm start",
    "dev:bot": "cd bot && npm run dev",
    "dev:dashboard": "cd dashboard && npm run dev",
    "dev": "concurrently \"npm run dev:bot\" \"npm run dev:dashboard\"",
    "setup": "cp bot/.env.example bot/.env && echo 'Setup complete. Edit bot/.env with your config.'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
